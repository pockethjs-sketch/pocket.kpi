# 신규 계약 → 프리미팅 기업 자동 반영

확인일: 2026-09-15 (KST). 금액 기준은 사용자 확인: **별도가 500 → 계약액 550만원**.

## 원천과 저장 경로

- 읽기 전용 원천: Google Sheet `1vNlj417n1JE0HTOky5FCKS0_Y5daeor7oIok3SXMbss`, `계약 프로세스`, gid 0.
- 실제 헤더: A 날짜 / B 업체명 / C 프리 / H 대금 지급 확인 / N 프로젝트 / O 별도가 / P 비고 / Q 신규/기존. H열의 완료 표시는 별도 입금 확인 규칙으로 반영한다.
- 과거 파서는 N을 금액 포함 텍스트, O를 비고, P를 신규/기존으로 잘못 읽었다. 이제 필수 헤더 이름으로 찾고 누락·중복이면 오류로 중단한다.
- `시트 동기화` 버튼 또는 매일 한국시간 09시 이후 서버 작업 → Apps Script POST `contract_auto_sync` / 동일 서버 함수 → Supabase 최신 정본 조회 → 변경 patch와 활동 로그를 같은 저장 요청으로 COMMIT → 같은 리비전의 bootstrap/crm을 재조회.
- 9/15 후속 요청으로 페이지 진입·5분 간격 자동 쓰기를 제거했다. 페이지를 닫아도 일일 작업이 실행된다. 최초 예약일은 9/16이며, 5분 시계 확인 중 당일 09시 이후 첫 실행에서만 원천 동기화한다. 광고 일일 수집 및 15분 Sheets 백업 일정은 그대로다. 세부 사항은 [DAILY_SYNC.md](DAILY_SYNC.md).
- 운영 정본은 KPI 전용 Supabase. 시트는 계약 원천 및 비동기 복구 백업이며 원천 계약 프로세스 시트를 수정하지 않는다.

## 자동 처리 대상

1. 시트 계약일 2026-08-01 이후부터 실행 당일까지, 해당 업체의 모든 계약 행이 명시적으로 `신규`.
2. 프리미팅 일정/내방 기록이 있고 상태가 프리미팅 확정·완료, 견적·제안 발송, 계약 완료인 저장 업체.
3. 회사명 또는 괄호 별칭이 정확히 일치하는 1:1 연결. 법인 표기·공백·문장부호 정규화만 사용하며 유사도 추정은 하지 않는다. 복합 업체명(`/` 등)과 중복 매칭은 자동 반영하지 않는다.
4. 대상 업체도 신규여야 한다. 계약일·프로젝트·금액이 유효해야 한다. 복수 계약일·중복 행·추가/반복 계약 의심·금액 불명은 보류한다.

| 항목 | 저장 규칙 |
|---|---|
| 별도가 | 숫자는 만원 단위. 부가세 10% 포함 후 원 단위 반올림. `원` 단위 표기는 직접 환산. `(포함가)` 등 명시 시 추가 VAT 없음 |
| 계약액 | `contractAmount`. 최초에는 비어 있거나 0인 값만 보완. 기존 값이 같으면 허용, 다르면 보류 |
| 계약일 | `contractAt`. 위와 같은 기존 입력 보호 |
| 상태 | `계약 완료`. 계약 수·계약액·ROAS 집계에도 영향 |
| 프로젝트·비고 | `memo`에 `[계약 프로세스 자동연동]` 구획 추가. 기존 사용자 메모는 유지 |
| 빌드업 | 원본 프로젝트에서 확실히 분류되고 기존 필드가 비어 있을 때만 보완 |
| 기존 입금·중도금·잔금 | 계약 자동 보완에서는 보존. 별도 H열 완료 규칙에 부합하는 단일 미납 회차만 아래처럼 확인. 계약액이 기존 입금액보다 작으면 보류 |
| 견적·영업담당·내방일 | 기존 `expected`, 담당자, 프리미팅 기록 유지 |
| 동기화 이력 | 리드 `contractSheetSync`에 원본 행·의미 해시·마지막 반영값·시각. `contractStatusLogs`에 `외부 시트 자동 반영`과 전후값 |

이후 원본 값이 바뀌면, 현재 값이 마지막 자동 반영값과 같은 필드만 갱신한다. 사용자가 별도로 수정한 금액·계약일은 덮어쓰지 않는다. 같은 원본 재조회, 행 이동, 입금 체크박스 변경만으로 계약을 다시 적용하지 않는다. 원본 행이 줄거나 삭제되어도 고객 삭제·금액 감소를 전파하지 않는다.

### H열 입금 완료 확인 (2026-09-17 추가)

- H열이 정확히 `✔` 또는 명시적 `입금 완료`/`지급 완료`인 경우만 후보. 보류·리마인드·정기 결제 문구는 완료가 아니다. 원본 계약액은 앞서 확인한 부가세 포함 환산 기준이다.
- 동일 원본/DB 업체 고유 1:1, 원본 계약행 하나, DB 상태 `계약 완료`, 원본 계약액과 DB `contractAmount` 및 유일한 미납 결제 회차 금액이 모두 같아야 한다. 계약액과 결제 회차가 맞지 않거나 기존 부분 입금·복수 계약·CRM 관리 회차·중복 업체는 자동 반영하지 않고 보류 사유를 표시한다.
- 해당 회차에 `paidConfirmed=true`와 누적 `paid=계약액`을 한 DB mutation으로 저장한다. `paidAt`은 빈칸 그대로 둔다. H열에는 실제 입금일이 없으므로 계약일이나 동기화일을 실제 입금일로 만들지 않는다. 수금 현황·미수금은 바뀌지만 입금일 기준 월매출은 별도의 날짜 확인 전까지 바뀌지 않는다.
- `contractSheetPaymentSync`에 원본키·확인 증거 해시·확인시각·실입금일 미상임을 기록하고 동일 mutation의 `contractStatusLogs`에 금액 전후를 남긴다. 이미 완료된 회차는 재반영하지 않는다. 이후 H열이 빈칸으로 바뀌어도 완료를 자동 취소하지 않으며 사람이 원천을 확인해야 한다.
- 계약 자동 보완으로 같은 요청에서 계약액이 새로 채워진 경우에도 위 조건을 충족하면 결제 확인을 하나의 원자 저장으로 묶는다. 원천 시트와 기존 입금 날짜·사용자 메모·기타 회차를 고치지 않는다.

미등록 업체는 기존처럼 `신규업체 생성 / 제외` 미리보기로 처리하며 자동 생성하지 않는다. 이 큐의 기존 업체 대조도 지연될 수 있는 Sheets 원장이 아니라 Supabase 정본을 사용한다.

## 저장 안전장치

- 기존 ScriptLock 및 Supabase base revision/멱등 mutation 경로 사용. 충돌하면 최신 DB로 후보와 패치를 다시 계산한다(최대 3회). 오래된 금액 패치를 그대로 재전송하지 않는다.
- 화면 재조회는 두 도메인의 리비전 일치를 확인하며 Sheets 복구본으로 후퇴하지 않는다.
- 재조회 중 사용자 미저장 변경을 최신 정본에 다시 병합하고 기존 브라우저 저널·저장 큐를 통해 저장한다. 별도 마케팅 값은 보존한다.
- 서버 성공 및 DB 재조회 전에는 성공 표시하지 않는다. HTML 응답·통신 실패 시 기존 값과 마지막 결과를 남기고 실패를 표시한다.
- 보류 목록에 업체명·이유를 표시한다. 사람이 판단할 계약을 임의로 덮어쓰지 않는다.

## 구현과 배포

- 테스트 대상 순수 로직: `scripts/contract-sheet-sync-core.mjs`.
- `node scripts/build-contract-sync.mjs`가 같은 로직을 `../pocket-kpi-deploy/Code.gs`의 GENERATED 구간에 기계적으로 삽입한다. GS 복사본을 여러 개 만들지 않는다.
- 서버 통합: `_crmContractSourceGroups`, `_crmAutoSyncNewContracts`, POST 라우트. 기존 `_crmContractSourceGroupsLegacy`는 호출하지 않는 보관 코드.
- 최초 자동 계약 반영 배포는 버전 56. 일일 동기화 후속 배포는 같은 배포 ID의 **버전 58**, backend `2026-09-15-daily-sync-v28`. Code와 crm_lead를 갱신했고 마케팅·백업 파일 및 권한 설정은 보존.
- 프런트: `src/main.jsx`의 동기화 호출·DB 재조회·프리미팅 기업 상태 표시. DB 스키마 및 Edge Function 변경 없음.
- 웹 배포 결과는 `PROJECT_STATE.md`에 기록한다.

## 운영 검증 및 복구

- 9/15 최초 작업은 실제 원본 153행과 운영 DB 776개 리드로 미리보기 후 **12곳 반영, 17곳 보류**.
- 9/17 H열 추가 변경: Apps Script 동일 배포 ID 버전 59/backend `2026-09-17-contract-payment-v29`. 운영 dry-run에서 계약 변경 0·입금 후보 6·입금 보류 22를 확인한 뒤 6곳 22,957,000원을 단일 리비전 `20260917003134697-29x3`에 적용했다. 재조회에서 결제 확인/누적 입금·활동 로그 6건 확인, 리드 801개 유지·대상 외 795개 변경 0. 재실행 dry-run 입금 변경 후보 0. 실제 입금일은 모두 원본에 없으므로 비워 둠.
- 보류 22곳은 전체 원본 H열 완료표시 중 계약/회차/업체 대조가 맞지 않는 건이다. 소스의 H열이 비어 있거나 보류 표시인 업체는 이 22곳에 포함되지 않는다. 검토 없이 `paidAt`을 계약일로 대체하거나 금액 차액을 입금 처리하지 않는다.
- COMMIT 리비전 `20260915080053524-54mg`; 계약액 총 증가 **63,045,400원**. 신규 고객 생성 없음.
- 반영 후 DB를 재조회해 12곳 계약액·계약일·상태·메모 확인. 12개 활동 로그 확인. 모든 입금·잔금·기존 이력 및 대상 외 모든 업체는 전후 동일함을 검증.
- 재실행 dry-run: 추가 변경 0, 동일 원본 12, 보류 17, 리비전 동일.
- 격리 화면 QA: 550만원 표시, 보류 사유, 통신 실패, 데스크톱·모바일 확인. 운영 결제 입력을 테스트 목적으로 만들지 않았다.
- 사전 서버 코드·배포 설정·원본·DB 및 적용 후 조회는 git-ignored `artifacts/contract-auto-backup/`에 보관. 개인정보가 있는 백업은 공개 GitHub·웹 빌드에 넣지 않는다.
- 되돌릴 때는 저장된 전후 차이를 최신 DB와 비교하고 **이번 작업 필드만 역패치**한다. 전체 스냅샷으로 현재 사용자 입력을 덮어쓰지 않는다. 코드 롤백만으로 이미 저장한 값이 취소되지는 않는다.
- Sheets 15분 비동기 백업의 이번 리비전 완료 여부는 이 기능 검증과 별개이며 별도 확인 전에는 완료로 표시하지 않는다.
